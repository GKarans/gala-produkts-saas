# Launch gates

No public-sales readiness claim is made. These checks must be completed before enabling production. None requires spending money without a separate owner decision.

For the Cloudflare domain handoff, candidate isolation, and production-safe
rollback sequence, use [CLOUDFLARE-CUTOVER.md](CLOUDFLARE-CUTOVER.md). Do not
use the closed-test Worker/version as a production rollback after
accepting real customer data. Production rollback must stay on production
database and storage resources.

For owner-run auth, photo-access, backup/restore, load, phone and DNS/TLS
checks, follow [OWNER-VERIFICATION-GUIDE-LV.md](OWNER-VERIFICATION-GUIDE-LV.md).

The closed-test Worker currently uses the explicit staging-mode approval pair
(`PLATFORM_RELEASE_APPROVED=staging` and `PLATFORM_MODE=staging`); this is a
mode name, not a remaining staging deployment. Production requires the matching
production approval pair. Set it only after every required
engineering and owner-dependent gate below has dated evidence and explicit
owner approval. A matching environment flag is a release switch, not proof that
the gates passed.

## Current closed-development-domain status (2026-09-25 17:27 UTC)

- Domain/browser incident recheck (2026-09-25 17:50 UTC): the workstation's default DNS resolver and Cloudflare `1.1.1.1` both returned Cloudflare A records (`104.21.67.110`, `172.67.221.99`). Normal `curl.exe -I https://lumiq.cam` completed TLS validation and returned the expected Cloudflare Access `302` challenge; the closed-test `workers.dev` host did the same. The live Worker tail contained successful API and scheduled requests through 20:49 Europe/Riga, with no visible failed requests in the captured tail. The owner then confirmed the site works after retry/reload. This establishes recovery but not the cause of the transient app-level “Connection interrupted” state. A broken logo image was visible in the screenshot; the current checkout contains `public/assets/brand-mark.png`, but deployed asset delivery was not independently checked behind Access. No DNS, Access or Worker setting was changed.
- Repeat live upload check (2026-09-25 17:55 UTC, existing synthetic event; no extra publication slot): selected the repository's generated `garden-gathering.webp` through the guest page. Guest UI reached `Photo uploaded!` / `1 uploaded · 0 need attention`; organizer gallery refresh showed `2 of 2 photos`. The Cloudflare dashboard's exact photographer prefix showed two `.webp` originals (109.69 KB and 335.63 KB) under `guntars-karans-468827/events/synthetic-r2-verification-2026-09-25-cc8d61/synthetic-guest-6706bc/`; nested `thumb/` contained the two matching thumbnails (10.57 KB and 28.99 KB). Worker tail confirmed reservation, original PUT, thumbnail PUT, finalize and both thumbnail GETs as `Ok`. This is a successful generated-image flow, not a physical phone camera or multi-device/network interruption test. Both disposable synthetic photos remain for the scheduled event-end ZIP verification.

- Owner-approved closed-pilot cutover: Cloudflare shows `lumiq.cam` and `lumiq-closed-test.gkarans-events.workers.dev` attached to `lumiq-closed-test`, version `4fc5b617-5815-4c0e-bb2d-e0492279d683` at 100%. It uses only the closed-test Supabase/Hyperdrive and private `lumiq-closed-test-photos` bucket; runtime mode remains staging/test, not production.
- Cloudflare Access has separate self-hosted applications for both hostnames, using the same owner-only policy with the exact email `guntars.karans@gmail.com`. The dashboard confirmed the One-time PIN login method now shows `ADDED` after reload, alongside the Cloudflare identity provider. Actual PIN delivery and fresh OTP login are not verified. Anonymous requests to both hosts returned HTTP 302 with normal TLS verification (`ssl_verify_result=0`); this verifies the Access challenge and certificate from this shell, not email delivery.
- DNS/TLS recheck (17:05 UTC): default DNS and `1.1.1.1` both returned Cloudflare addresses `104.21.67.110` and `172.67.221.99`. Ordinary `curl.exe https://lumiq.cam/healthz` failed TLS verification with Windows Schannel `SEC_E_UNTRUSTED_ROOT`; the same request pinned diagnostically to Cloudflare `104.21.67.110` passed normal certificate verification and returned Access 302. The closed-test `workers.dev` hostname also passed normal TLS and returned Access 302. No certificate check was bypassed. This points to a differing local/network TLS path, but does not identify its cause; the earlier Tet warning was not recreated in this check and formal Tet classification clearance is unknown. Authenticated health remains unverified.
- `lumiq-cam`, its staging Hyperdrive (`lumiq-supabase`, old Supabase ref `mkycfegxlzidaezljutq`), and both same-named `lumiq-staging-photos` buckets in default and EU jurisdictions are now deleted. The old Supabase staging project deletion is owner-reported, not independently verified. The checked-in legacy Wrangler config was removed to prevent references to deleted resources; npm deploy/dev aliases remain blocked.
- The owner reports that the previously disposable closed-test photos were removed and every earlier test event was manually deleted after emptying `lumiq-closed-test-photos`. One owner-approved synthetic event was subsequently published for R2 verification; its test evidence and remaining test data are recorded below. The dashboard's bucket aggregate (917.31 KB) and Wrangler's cached bucket summary (7 objects / 582 kB) were stale during checks; use the prefix object table as the inventory. `app-images` was not touched and the separate `event-photo-media` Worker remains.
- Closed-test photo verification (2026-09-25 17:18–17:21 UTC, `lumiq.cam` behind Access): owner session, dashboard and event gallery loaded; before creating the new event the dashboard showed no events/photos. With explicit owner approval to consume one Gathering event slot, published synthetic event `Synthetic R2 Verification 2026-09-25` (`cc8d61dd-9b02-4c68-a4a3-0069f098bf6a`). Two synthetic uploads by `Synthetic Guest` both reached 100%; organizer gallery showed `2 of 2 photos`. Both original `.webp` objects appeared under `guntars-karans-468827/events/synthetic-r2-verification-2026-09-25-cc8d61/synthetic-guest-6706bc/`; both matching thumbnails appeared under that guest folder's `thumb/`. Downloading one original returned Worker `Ok`. Deleting that photo returned `DELETE /api/events/{event}/photos — Ok`; the refreshed gallery showed `1 of 1 photos`. After the next scheduled cleanup run returned `Ok`, the deleted original and matching thumbnail were absent from R2; the other original and thumbnail remained. Worker tail contained successful session, event, gallery, thumbnail, upload reservation, original/thumbnail writes, finalize, download, delete, and scheduled-cleanup requests, with no logged request/cron errors during the observed interval. Log retained at `%TEMP%\lumiq-closed-test-tail-20260925-1718.log`. This is one small synthetic closed-test flow, not production, mobile-device, concurrency, load, backup-restore, or security-review evidence. One synthetic photo plus its thumbnail remains in the test bucket. The event was briefly archived and restored to test those controls; the app correctly restored the upcoming event as a draft, retaining its existing publication ledger. It was then republished using the existing event allowance (no second slot) and is live again until 21:10 Europe/Riga. Its event-time edit is intentionally blocked after photos have been submitted.
- Closed-pilot flow continuation (2026-09-25 17:38–17:44 UTC, `lumiq.cam` behind Access): account subscription changed only in test Supabase project `cpweowosocjuccjsyyic` from unpaid `Gathering trialing` to unpaid `Studio trialing`; the current period end was preserved and both provider IDs remain null. Payments are disabled and no order/provider subscription was created. Existing event entitlements remain snapshots. Billing UI verified `4 / 12` publications used and `8` remaining after the next synthetic event was published. Created `Synthetic Short Flow Test 2026-09-25` (`374a618e-7a89-480d-b712-614d6565f443`), scheduled 20:38–20:43 Europe/Riga, with `City rooftop` cover. It was published with the Studio allowance; no photo is uploaded to this second event. Its guest page accepted synthetic name `Synthetic Flow QA`; the guest joined successfully, and after event end the page showed the event closed. No physical camera/file-picker upload was done. Live design-isolation check: its saved QR layout is `Modern frame`, while the earlier event remains `Garden celebration`; closing/reopening preserved the new event's design. Its visible cover is City rooftop, while the earlier event still shows Garden gathering. This verifies different built-in presets remain event-specific, not uploaded Canva-artwork independence. After the short event ended, sharing was enabled for its empty synthetic gallery; the guest link showed the shared-gallery view with `0 of 0 photos`. Sharing was then revoked and reloading the guest URL returned to `This event is closed`; no photos were exposed. This verifies toggle/revocation, not shared-photo access.
- Closed-test upload, ZIP, sharing and revocation follow-up (2026-09-25 20:55–21:27 Europe/Riga): two generated photos uploaded to the already-approved `Synthetic R2 Verification 2026-09-25` event; organizer gallery showed both, and R2 contained each original under the photographer folder plus its matching nested `thumb/` WebP. At event end, the automatic two-photo ZIP remained queued while minute Cron executions repeatedly reported `Exceeded CPU Limit` from 21:06 through 21:15. Limited DB-polled scheduled execution to one job per invocation (`concurrency: 1`, `maxJobs: 1`), deployed only closed-test Worker version `8f6c4f83-703d-49e2-821c-8895abdadcb8`; the next Cron at 21:16 and following cycles returned `Ok`. The export reached `Automatic · 2 photos ready`, available until 2026-10-09 21:10, and the part-download request returned Worker `Ok`. Deleted one gallery photo after archive creation: gallery fell to `1 of 1`, R2 retained only the other original and thumbnail, and Exports still showed the same `2 photos ready`; this verifies post-end gallery deletion does not rebuild or remove the immutable ZIP. Then enabled event sharing, opened the guest link in the same Access-authenticated browser, saw the remaining photo, opened full-size preview and downloaded it (Worker `Ok`); revoked sharing and reopened the link, which returned `This event is closed.` This verifies the app's share/revoke behavior from an Access-authenticated session, not an anonymous/external guest session or cross-organizer isolation. Not verified: archive extraction/checksum, Free-plan capacity at scale or production readiness. Worker tail retained at `%TEMP%\lumiq-closed-test-tail-20260925-1718.log`.
- Fresh read-only CLI check at 16:54 UTC: R2 lists only `app-images` and `lumiq-closed-test-photos`; Hyperdrive lists only closed-test ID `6823aef81a314970bd3da962c2a62966`; Queue list is empty. `lumiq-cam` deployment lookup returns Cloudflare error 10007, “This Worker does not exist on your account.” `lumiq-closed-test` deployment history shows version `4fc5b617-5815-4c0e-bb2d-e0492279d683` at 100%. This confirms those resource states, not current Access login or authenticated health.
- Fresh read-only Cloudflare inventory (2026-09-25 17:27 UTC): `lumiq-closed-test` remains at 100% version `4fc5b617-5815-4c0e-bb2d-e0492279d683`; R2 lists only `app-images` and `lumiq-closed-test-photos`; Hyperdrive lists only closed-test `6823aef81a314970bd3da962c2a62966` connected to Supabase project `cpweowosocjuccjsyyic`; Queue list is empty. No production Worker, Hyperdrive, R2 bucket or Queue is provisioned. Read-only inventory only; no live route or resource was changed.
- Current-worktree verification (2026-09-25 18:24 UTC): `npm run check` passed security scan (183 tracked files), `npm audit` (0 vulnerabilities), 103/103 tests, 55-file build, responsive browser checks at 320, 360, 375, 390, 430, 500, 768, 1024 and 1440 px; organizer/guest/design/billing/refinement journeys; axe, keyboard, reduced-motion and 200% zoom. This verifies local source only; production resources, independent security review, real Auth callbacks, phone testing, restore drill and live load remain unverified.
- This remains an Access-gated development pilot, not production. No production Supabase project, Hyperdrive, R2 bucket, Queue, paid capacity approval, backup restore drill, independent security review or company/legal sign-off exists. Keep production locked.

## Historical deployment baseline (2026-09-25; superseded by current status above)

- Historical closed-test retest (2026-09-25 12:53 UTC): `lumiq-closed-test` was at 100% version `14201856-ee27-4ad7-b159-2903c93f8c6d`, built with only test Hyperdrive `6823aef81a314970bd3da962c2a62966` and private R2 bucket `lumiq-closed-test-photos`. A generated `party.webp` upload completed through the guest flow. R2 showed the original in the photographer folder and its thumbnail under that guest folder's `thumb/`. The two earlier event-root photos were deleted through the gallery; after the six-minute cleanup delay, R2 showed neither their root-level originals nor the old root-level `thumb/`, while the new guest-folder original and thumbnail remained. At the event's 15:40 Europe/Riga end, the automatic archive initially failed because R2 requires a known-length readable stream. The Worker was fixed to calculate the ZIP size, upload through `FixedLengthStream`, and enforce the configured byte ceiling. After retry, Exports showed `Automatic · 1 photos ready`, available until 2026-10-09 15:40 Europe/Riga. A follow-up test-only deployment `79cf3383-7f49-475b-a9b5-b4e7fa2e5b5a` adds dynamic organizer/event ZIP download names, including for an already-ready archive. This verifies one small archive and one event in closed test only; it does not prove production load capacity. `lumiq-cam` remained at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e`.
- Historical closed-test deployment check (2026-09-25 12:01 UTC): Worker `lumiq-closed-test` was at 100% version `7ff91957-912b-4c1a-b967-4ab34900bd44`. Its dry-run configuration resolved only test Hyperdrive `6823aef81a314970bd3da962c2a62966` and `lumiq-closed-test-photos`; it is protected by Cloudflare Access. Authenticated organizer UI loaded the synthetic event gallery with all 3 test photos and no browser-console errors after deployment. Tail observed successful config/session/event/gallery/thumbnail GETs and scheduled work, but no photo deletion was submitted. Direct shell `/healthz` was intercepted by Cloudflare Access, so health was not independently confirmed in that check. The separate `lumiq-cam` deployment remained at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e`; the test deployment did not alter it.
- Historical verified snapshot (2026-09-25 14:57 UTC): `lumiq-cam` was at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e`, using staging Hyperdrive `96846aaf46fd470e96f0313b09e1d01a` and `lumiq-staging-photos`. The separate `lumiq-closed-test` Worker was at 100% version `2e644352-2e50-4e3d-8618-e65336323824`; the owner verified its exact `/healthz` identity and `database: ready`, `storage: bound`, and anonymous Access redirects were enabled. Authenticated UI smoke after that version loaded the organizer dashboard and the synthetic layout test event; its completed gallery showed `1 of 1 photos` and the thumbnail rendered. At that time, `lumiq.cam` `/app` returned 200 and unauthenticated `/api/events` returned 401; its app shell and health were not behind Access. Cloudflare had no production Hyperdrive, private production bucket, or Queue, and there was no separate production Supabase project. Historical timestamped evidence follows.
- The separate `lumiq-cam` Worker was at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e` before the closed-pilot hostname cutover. Its checked-in configuration routes that Worker to staging mode, staging Hyperdrive `96846aaf46fd470e96f0313b09e1d01a`, and `lumiq-staging-photos`. It is detached from `lumiq.cam` and is not a valid production or closed-pilot rollback target.
- Anonymous `GET /healthz` returned HTTP 200 at `lumiq.cam` on 2026-09-24, while the same request to the closed-test `workers.dev` hostname returned HTTP 302 with `Www-Authenticate: Cloudflare-Access`. The root health endpoint is not behind Access; authentication behavior for the other root paths still needs a path-by-path check. Configure and verify a root-domain Access policy before serving the closed test there.
- DNS/TLS check on 2026-09-25: the workstation's configured resolver (`192.168.1.254`) returned `81.198.92.113` for `lumiq.cam`, and normal `curl.exe` then failed with `SEC_E_UNTRUSTED_ROOT`; certificate verification was not bypassed. Independent queries to Cloudflare `1.1.1.1` and Google `8.8.8.8` returned Cloudflare A records `104.21.67.110` and `172.67.221.99`, with nameservers `demi.ns.cloudflare.com` and `mario.ns.cloudflare.com`. With normal certificate validation still enabled and `curl --resolve` pinning the hostname to each of those Cloudflare IPs, both `https://lumiq.cam/healthz` requests returned HTTP 200; the body reported `service: lumiq-cam`, `database: ready`, and `storage: bound`. The active `lumiq-cam` Worker is independently confirmed at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e`. Public Cloudflare DNS/TLS and root health are therefore verified from this check; the local resolver mismatch remains unexplained and should be corrected or investigated by the owner before relying on this workstation's ordinary browsing. No DNS, Worker, certificate or domain setting was changed.
- Recheck on 2026-09-25 13:13 UTC: `wrangler deployments list` again showed closed-test Worker `lumiq-closed-test` at 100% version `79cf3383-7f49-475b-a9b5-b4e7fa2e5b5a` and `lumiq-cam` at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e`. Cloudflare DNS `1.1.1.1` returned `104.21.67.110` and `172.67.221.99`; requests to `https://lumiq.cam/healthz`, pinned individually to both addresses with normal TLS verification, returned HTTP 200 and `{"status":"ok","service":"lumiq-cam","database":"ready","storage":"bound"}`. The workstation's default resolver now returned `195.122.12.177`, and ordinary `curl.exe` failed TLS validation with `SEC_E_UNTRUSTED_ROOT`. This confirms the current public Cloudflare path and test/live Worker version split, but the workstation resolver discrepancy is still unresolved. No deployment, DNS, certificate or domain setting was changed.
- Cloud resource inventory recheck on 2026-09-25: Cloudflare `wrangler r2 bucket list` returned `app-images`, `lumiq-closed-test-photos` and `lumiq-staging-photos`; there is no dedicated production bucket. `wrangler hyperdrive list` returned only `lumiq-closed-test` (`cpweowosocjuccjsyyic`) and `lumiq-supabase` (staging project `mkycfegxlzidaezljutq`); there is no production Hyperdrive. `wrangler queues list` returned no queues. Independent DNS query through Google `8.8.8.8` matched Cloudflare `1.1.1.1` at `104.21.67.110` and `172.67.221.99`; only the workstation's configured resolver disagrees. These are read-only inventory checks. Production infrastructure and Queue-backed load acceptance remain incomplete; no resource, plan, DNS or Worker was changed.
- Local DNS follow-up: the Windows Wi-Fi adapter is configured to use `192.168.1.254`, which currently resolves `lumiq.cam` to `195.122.12.177`, unlike both independent public resolvers; TLS validation at that address fails with `SEC_E_UNTRUSTED_ROOT`. Owner/operator follow-up is to inspect the router's DNS forwarding/cache or use a trusted public resolver and repeat the ordinary hostname/TLS check. Never work around this by disabling certificate validation.
- DNS/TLS recheck on 2026-09-25 14:47 UTC: ordinary `curl.exe https://lumiq.cam/healthz` still fails with `SEC_E_UNTRUSTED_ROOT`. The workstation's default resolver returned `81.198.92.113`, while Cloudflare `1.1.1.1` and Google `8.8.8.8` both returned `104.21.67.110` and `172.67.221.99`. With normal TLS verification enabled and `--resolve` pinning each Cloudflare IP separately, both requests returned `{"status":"ok","service":"lumiq-cam","database":"ready","storage":"bound"}`. This confirms the public Cloudflare route, TLS and staging Worker health while the local resolver path remains unresolved. No certificate validation was bypassed; no DNS or Worker setting was changed.
- DNS/TLS recheck on 2026-09-25 15:31 UTC: on the owner's normal network, `Resolve-DnsName lumiq.cam -Type A` returned `195.122.12.177`; the same query to `1.1.1.1` returned Cloudflare IPs `104.21.67.110` and `172.67.221.99`. Ordinary `curl.exe https://lumiq.cam/healthz` still failed normal TLS verification with `SEC_E_UNTRUSTED_ROOT` (HTTP 000). No TLS validation was bypassed and no DNS, Worker or certificate setting was changed. Tet review remains necessary.
- DNS/TLS isolation recheck on 2026-09-25 15:35 UTC: the default resolver again returned Tet address `195.122.12.177`; `1.1.1.1` returned `104.21.67.110` and `172.67.221.99`. A normal TLS-verified request pinned to Cloudflare `104.21.67.110` reached `lumiq.cam` and received the expected Cloudflare Access HTTP 302. The normal `workers.dev` test hostname independently returned the same Access 302 with normal TLS. This isolates the owner's reported failure to the Tet DNS/security path rather than Cloudflare's custom-domain certificate or Access routing. IP pinning was diagnostic only; do not use it to bypass Tet in normal browsing. The `workers.dev` host remains the supported owner testing route while Tet investigates.
- Follow-up to the owner's `workers.dev` screenshot: the exact “This host is not allowed.” text is emitted by the app's strict single-origin check. Access's anonymous 302 proves only that Access challenged the request; after Access, the application rejected the `workers.dev` Host because its configured canonical origin was `https://lumiq.cam`. Added an exact secondary-origin allowlist and kept CSRF Origin checks exact. The closed-test config now uses its own `workers.dev` origin for QR/Auth return links and explicitly allows `https://lumiq.cam` as the only alternate. Version `4fc5b617-5815-4c0e-bb2d-e0492279d683` was deployed to the closed-test Worker only; production and staging were not deployed. Recheck authenticated browser access and real email callbacks. This fixes the test-host application error, not Tet's DNS interception of `lumiq.cam`.
- Historical closed-test deployment (2026-09-25 14:01 UTC): Worker `lumiq-closed-test` was at 100% version `040fa44f-79fd-46bf-b60e-4d1d9d2b3b1f`, isolated to test Hyperdrive `6823aef81a314970bd3da962c2a62966` and private R2 `lumiq-closed-test-photos`, with Access still enabled. The deployed public home page rendered; authenticated browser requests to `/api/config` and `/api/auth/session` succeeded. The production code now checks the Auth URL against an explicit non-secret `PLATFORM_SUPABASE_PROJECT_REF`: Hyperdrive's runtime connection string includes a generated runtime username, so the origin project must instead be verified using `wrangler hyperdrive get` and the configured reference. Full `npm run check` passed 94/94 tests, build, responsive browsers and accessibility before deployment. This does not prove real mailbox flows or production behavior.
- Historical pre-cutover `lumiq.cam` recheck (2026-09-25): `lumiq-cam` was at 100% version `4e9ace8c-dc93-4451-85e2-2e03bbe8138e`, bound to staging Hyperdrive `96846aaf46fd470e96f0313b09e1d01a` and `lumiq-staging-photos`. Public DNS via `1.1.1.1` resolved to Cloudflare `104.21.67.110` and `172.67.221.99`; this workstation's default resolver returned `81.198.92.113` (TTL 1). A normal-TLS request pinned to Cloudflare returned `database: ready` and `storage: bound`. Unauthenticated requests to `https://lumiq.cam/app` returned 200 (public app shell) and `/api/events` returned 401 (API denied anonymous access). The closed-pilot Access policy and domain move were performed later and are recorded above.
- Historical account resource inventory (2026-09-25, superseded by current status): R2 then contained `app-images`, `lumiq-closed-test-photos`, and `lumiq-staging-photos`; Hyperdrive then contained the closed-test and staging configs; `wrangler queues list` returned no Queues. No production Supabase project, Hyperdrive, R2 bucket or Queue existed. No paid-resource provisioning or `lumiq.cam` change was performed in that snapshot.
- Historical retest on 2026-09-25, before staging cleanup: `wrangler r2 bucket list` then showed only
  `app-images`, `lumiq-closed-test-photos` and `lumiq-staging-photos`;
  `wrangler hyperdrive list` then showed the closed-test project
  `cpweowosocjuccjsyyic` and staging project `mkycfegxlzidaezljutq`; no
  Cloudflare Queues existed. Deployment history then showed closed-test Worker
  `040fa44f-79fd-46bf-b60e-4d1d9d2b3b1f` at 100% and `lumiq-cam` Worker
  `4e9ace8c-dc93-4451-85e2-2e03bbe8138e` at 100%. The owner then confirmed
  guarded migration `012-tier-photo-capacity` and all 12 versions verified in
  the closed-test DB, with RLS enabled and anon/authenticated SELECT denied on
  17 tables; no MVP migration ran. A closed-test dry-run resolved only its
  test Hyperdrive and private R2 bucket, then version
  `0d6c9cd2-3d03-44bc-8bb5-a4591bde7fc9` was deployed at 100%, then a
  health-label fix was deployed as `2e644352-2e50-4e3d-8618-e65336323824`,
  also at 100%. The owner reported healthy DB/storage on `/healthz` after the
  first deployment, but that version's hard-coded service label could not
  identify the hostname. Recheck the hostname-specific health response and
  authenticated UI after the second deployment. `npm run check`
  passed locally on this source: secret scan across 184 tracked files, zero `npm audit` findings,
  100/100 Node tests, 55-file build, browser widths 320-1440 px, user journeys
  and accessibility checks. These checks do not substitute for live test DB
  migration, live deployment, or production evidence. No production resource,
  DNS, Worker or paid plan was changed. This inventory predates deletion of
  the staging Worker, Hyperdrive, and R2 buckets documented in the current status.

## Engineering work still requiring completion or target-environment evidence

- [x] Closed-test database foundation only: migrations `001-platform` through `012-tier-photo-capacity`, RLS on all 17 protected tables, anon/authenticated SELECT denied, and restricted `lumiq_runtime` role provisioned and privilege-checked. Evidence: owner ran the guarded migration verification and confirmed all twelve versions on the separate Free test project on 2026-09-25. No MVP migration ran. This is not production evidence.
- [x] Closed-test post-migration runtime check: on 2026-09-25 the owner opened the exact Access-protected `https://lumiq-closed-test.gkarans-events.workers.dev/healthz` and reported `{"status":"ok","service":"lumiq-closed-test.gkarans-events.workers.dev","database":"ready","storage":"bound"}`. This confirms the closed-test Worker identity, DB query and R2 binding only; it is not production evidence.
- [x] Current closed-test authenticated UI smoke (2026-09-25 14:57 UTC): in an authenticated Access browser session after Worker version `2e644352-2e50-4e3d-8618-e65336323824`, `/app` showed the organizer dashboard with four events and the Gathering plan. Opening `Synthetic R2 Layout Test 2026-09-25` displayed the completed event, `1 of 1 photos`, and its rendered thumbnail. Read-only; this does not verify browser console logs, guest upload, negative access cases or production behavior. Do not point `lumiq.cam` at this test stack.
- [x] Closed-test post-R2-deletion verification (2026-09-25 17:18–17:21 UTC): authenticated `lumiq.cam` dashboard/gallery and a newly published, owner-approved synthetic event were checked; 2 synthetic uploads appeared in the gallery and in the expected organizer/event/photographer/`thumb/` R2 layout. One original downloaded successfully; deleting it returned `Ok`, removed its gallery row, and the scheduled cleanup removed its original and thumbnail from R2. Worker tail showed no request or scheduled-job errors. Full evidence and remaining synthetic test data are recorded in the current status above; this is not production or physical-device evidence.
- [x] Closed-test item 1, photo upload/storage/gallery/delete/download (2026-09-25): synthetic originals and thumbnails uploaded to the expected private R2 photographer paths, rendered in the organizer gallery, one original was downloaded, and one photo was deleted with its original/thumbnail removed by cleanup. The event-end ZIP became ready with the original two-photo snapshot and remained unchanged after a post-end gallery deletion. One synthetic original plus thumbnail remains in the test bucket; no further test event or upload was created for this check. This is closed-test evidence, not production/mobile/load evidence.
- [x] Closed-test item 2, app-level access/privacy acceptance (2026-09-25): local platform tests verify guest access only with the event slug/token, photo access denied without guest authorization (`403`), another organizer receives `404` for the owner's event, share revocation denies the guest API (`403`), and a CSRF cross-origin mutation is denied (`403`). The focused platform suite was rerun: 12/12 tests passed. Live closed-test check with a second Lumiq organizer account: its dashboard showed `Explore`, zero events and zero photos; directly opening the first account's synthetic event route displayed `Event not found.` The same event's guest link, opened in that second app account after sharing had been revoked, displayed `This event is closed.` The bucket is configured private. This verifies the organizer event boundary and revoked guest-link UX in the closed-test app. It does not verify an anonymous Cloudflare Access session or a direct request for the specific remaining photo object, whose media ID was not available in the second account. Production identity-isolation acceptance remains separate.
- [x] Closed-test Hyperdrive connectivity: after Worker version `636cb1e2-2e0e-49d3-914e-47f335f0239f` reached 100%, the owner rechecked `/healthz` through Access and confirmed `database: ready` and `storage: bound` on 2026-09-23. This proves Worker startup checks and a database query through the separate test Hyperdrive; it is not production evidence.
- [x] Closed-test organizer smoke test: the owner signed into the protected app, created draft event `Balle`, and confirmed it remained after returning to the event. This proves a basic authenticated create/read path only; it does not prove guest flow, cross-environment isolation, Auth recovery or photo storage.
- [x] Closed-test billing page works with payment controls disabled: after version `b1b7fe9e-78a4-4e7a-a422-4a506d63608f`, the owner-authenticated page displayed that payments are disconnected, with no JavaScript page error. After the approved account-specific free test exception, the page showed `Gathering trialing`, `1 / 4` publications used and `3` remaining on 2026-09-24. This does not imply a paid plan or prove billing provider integration.
- [x] Worker release gate covers static assets, API/health and scheduled DB/mail work: local tests prove absent or mismatched mode/approval returns 503 before serving protected app behavior, while the explicitly approved closed-test mode is allowed. Latest source is deployed only to closed-test Worker `lumiq-closed-test` version `8f6c4f83-703d-49e2-821c-8895abdadcb8` at 100%; production remains locked behind an explicit production approval pair and no production configuration was changed. This is not production evidence.
- [x] Closed-test anonymous Access boundary rechecked on 2026-09-25 14:50 UTC: unauthenticated requests to `/`, `/app`, `/api/config`, `/api/events`, `/healthz`, `/auth/verify`, `/auth/reset`, `/auth/email`, `/api/auth/google/callback`, and synthetic guest event `/event/3e301d37c7c0aefadd5a3214d861a0fa` all returned HTTP 302 to Cloudflare Access. This confirms those paths on the closed-test hostname do not bypass its Access login, but does not verify authenticated sessions or Supabase email-link completion. The later `lumiq.cam` pilot cutover and Tet DNS/security warning are recorded in the current status above.
- [ ] Test-project Supabase Auth integration: the test Worker's `PLATFORM_ORIGIN` is `https://lumiq.cam`; the Supabase test project's Site URL and exact `/auth/verify`, `/auth/reset`, `/auth/email`, and `/api/auth/google/callback` redirects were updated for that hostname, retaining the same exact redirects on the closed-test `workers.dev` hostname. Signup/email confirmation are enabled; Google OAuth is disabled. Password recovery accepts both supported callback forms and requires a matching confirmation; automated tests cover these code paths. No real signup, mailbox delivery, password reset, email change or concurrent session refresh has been exercised against Supabase. Supabase uses default email templates and no custom SMTP is configured. The latest ordinary DNS/TLS probe reached the Access challenge, but authenticated login and callback flows remain unverified; the earlier Tet warning was not reproduced, and official Tet classification clearance is unknown. Run real Auth flows through Access and confirm each link lands on the intended page. Do not reuse test Auth settings in production.

  Verified closed-test Auth URL entries (production values are future configuration only):
  - Current test-project Site URL: `https://lumiq.cam`; this is a closed development pilot, not production.
  - Test-project allowed redirects: exact `/auth/verify`, `/auth/reset`, `/auth/email`, and `/api/auth/google/callback` paths on both `https://lumiq.cam` and `https://lumiq-closed-test.gkarans-events.workers.dev`.
  - Future production Site URL: `https://lumiq.cam`; allowed redirects are the same four paths on that origin, but only in the separately approved production Supabase project.
  - If Google OAuth is enabled, its Google provider's authorized callback is the Supabase Auth callback `https://<project-ref>.supabase.co/auth/v1/callback`; Lumiq's `/api/auth/google/callback` is the redirect target from Supabase back to the Worker.
- [ ] Remaining independent access/security verification: test the direct photo-object endpoint using an existing photo ID while authenticated as the second organizer, run a truly anonymous guest session with Access removed from guest routes in an isolated candidate, verify direct public R2 object denial, and exercise malformed/expired upload retries. The app-level item 2 is marked complete above based on local negative tests plus live separate-organizer route denial and share revocation; these residual items remain unverified and are not a production security sign-off.
- [ ] Prove the DB-shared limiter and trusted proxy/client-IP handling across actual closed-test Worker instances using isolated synthetic traffic; there is no staging deployment to test.
- [ ] Stripe sandbox renewal, delayed payment, changed plan, cancellation, taxes/invoices, failed payment, duplicate/out-of-order delivery and reconciliation. The local mock is not this evidence.
- [ ] Run the implemented numbered migration/checksum procedure against the production PostgreSQL target, including upgrade and backup/restore evidence. The closed-test schema was applied and verified, but no MVP migration or production migration was run.
- [x] Closed-test organizer/event/photographer photo key layout and thumbnail nesting verified in live R2 on 2026-09-25. A generated photo appeared under the photographer folder with its thumbnail under the nested `thumb/`; two obsolete synthetic event-root photo pairs were removed via the gallery and confirmed absent after cleanup. No customer photos were moved. This does not migrate legacy customer objects and is not production evidence.
- [x] Live-check event design isolation on two synthetic events (2026-09-25): changed and saved the newer event's built-in QR layout to `Modern frame`; after closing and reopening, it remained `Modern frame`, while the first event remained `Garden celebration`. The newer event retained its `City rooftop` cover while the first retained `Garden gathering`. Uploaded Canva artwork isolation remains untested; this verifies the event-specific built-in cover and QR selections in the deployed closed-test app.
- [x] No test-data migration is planned for production: the future production Supabase project and R2 bucket must start separately and empty. Do not copy `Balle`, closed-test records, or any closed-test objects to production. The owner reports manually deleting every closed-test event after the R2 bucket was emptied; the resulting gallery/metadata state has not been independently verified. No customer data exists in this test bucket. A future live upload test requires a newly created synthetic event and separate owner approval before consuming any event allowance.
- [ ] Upload memory profile on an older device with 20 mixed large JPEG/PNG/WebP and HEIC/HEIF files. Verify bounded server conversion, explicit fallback errors and IndexedDB queue recovery after refresh without persisting guest tokens.
- [ ] Validate alert webhook delivery, request-to-job correlation, failure classification and costs/support per event on an isolated closed-test candidate. Structured logs, request IDs, health checks and alert hooks are implemented; provider dashboards still need target-environment evidence.
- [ ] Cloudflare capacity/load test for maximum advertised event/ZIP allowances. The local Studio 1,000-photo maximum exports to two ZIP parts (about 72 MB), preserves every photo ID, and keeps queued ZIP stream data below 2 MiB under a deliberately slow local sink. Tests cover the immutable event-end snapshot, deletions around the event boundary, retry-safe cleanup and retention expiry. A new optional Queue-backed dispatcher now publishes at most 1,000 PostgreSQL job IDs/minute in Cloudflare-compliant batches of 100; DB polling remains as a no-binding fallback at up to 10 jobs/minute. For 100 Studio organizers, the theoretical maximum is 1,200 exports and up to about 9,600 parent/part jobs/month; Queue publication could take at least about 10 minutes at the configured dispatcher ceiling, but this is arithmetic, not measured completion latency. No Queue has been provisioned and no Queue-bound Worker has been deployed. Measure backlog, completion latency, CPU/memory, R2 operations, retries and failure recovery in an approved isolated Worker before claiming this capacity.
- [ ] Confirm Workers usage plan and measure actual CPU/query volume before ZIP/queue rollout. Current Cloudflare documentation lists 10 ms CPU per invocation on Workers Free; Standard/Paid provides 30 seconds default CPU (up to 5 minutes for HTTP/Queue consumers), with a $5 monthly base subscription, 10 million requests and 30 million CPU-ms included. The local 1,000-photo timing is wall time, not CPU time, and does not prove the closed-test Worker exceeds or fits its current plan. ZIP and queue load tests must capture per-invocation CPU, wall time, Hyperdrive query volume, retries and total plan charges. No Paid plan or other spend is approved by this gate. **Hyperdrive quota signal (owner email, 2026-09-25): 76% of the Free daily 100,000-statement limit reached, with reset at 2026-09-26 00:00 UTC.** The underlying usage panel was not readable in the available browser session; route/source attribution and recovery after reset are unverified. Treat as a real near-limit warning, not evidence of a particular endpoint or steady-state demand. Do not upgrade or claim capacity until the owner checks the usage panel after reset and an isolated, instrumented load test identifies the workload.
- [ ] Queue-backed scheduling in an isolated environment. Code now supports bounded Queue producer batches, message-ID validation, targeted DB claims, retry on configuration/startup failure, duplicate-safe processing, failed-message handling from a configured DLQ, and the existing scheduled DB polling fallback. Migration `011-queue-job-dispatch` is applied to the closed-test database, but no Queue or DLQ resource is provisioned and no Queue-bound Worker is deployed. Before a Queue-bound deployment, provision both queues and deploy only the closed-test config with the normal and DLQ bindings plus limited consumer concurrency. Test duplicate delivery, lost-message redispatch, retry/DLQ behavior, upstream DB limits, partial ZIP cleanup, archive latency and actual CPU. Queue resources are available on Cloudflare Free but have a 10,000 operations/day shared allowance and 24-hour retention; Workers Free has a 10 ms CPU limit, so actual ZIP processing may require a separately approved Workers Paid plan. No Queue resource or Paid plan was provisioned.
- [ ] Independent security review: IDOR, XSS, CSRF, auth recovery, RLS/DB role, signed URL replay, malformed images, plan/owner bypass and admin authorization.
- [ ] Item 3 in progress: actual database plus object backup/restore drill to an empty isolated environment, with checksums and sample ZIP verification. Backup format v3 records public/Auth user and identity row counts from the same exported PostgreSQL snapshot used by `pg_dump`; restore refuses outdated manifests and same-source/non-empty targets, compares restored row inventories and verifies exact restored R2 keys, sizes and SHA-256 checksums. On 2026-09-25, local backup and verification succeeded for 21 public/Auth tables and 3 R2 objects at `%LOCALAPPDATA%\Lumiq\backups\lumiq-restore-drill-20260925-221255`; the local verifier was rerun successfully. Source PostgreSQL version `17.6` and PostgreSQL 17 client tools were confirmed. The isolated Supabase Free project `sprzlvywzpeyuzbsyplz` (Central EU, Frankfurt) and R2 Standard bucket `lumiq-restore-drill-20260925` (EEUR) are created. Cloudflare dashboard verified the bucket has `Public Access: Disabled`, 0 objects and 0 B. A local `restore-local.ps1` now pins the target ref and bucket, re-verifies the backup, prompts for exact target confirmation, masks target credentials, enforces the target DB URL project ref, checks DB/Auth/bucket emptiness, and clears process secrets; DB passwords are kept out of PostgreSQL CLI arguments. Next: owner creates an Object Read & Write R2 token scoped only to the restore bucket, obtains its Supabase Session Pooler URL (5432), and runs the helper from a hidden local PowerShell prompt. No restore has run yet. Never point restore at the closed-test database or bucket. Migration verification, app smoke tests, sample ZIP extraction and cleanup of only the drill resources remain post-restore steps.
- [ ] Complete physical VoiceOver/NVDA and phone safe-area sign-off. Automated axe, keyboard entry, reduced motion, responsive viewports and 200% zoom checks already run in CI.
- [x] Local code-quality suite on the current working tree (2026-09-25): latest `npm run check` passed with 102/102 Node tests, secret scan across 184 tracked files, `npm audit` at 0 vulnerabilities, build validation of 55 current public files, browser checks at 320/360/375/390/430/500/768/1024/1440px, organizer/guest journey, designer, billing, refinement, axe, keyboard, reduced-motion and 200% zoom checks. Worker startup rejects mismatched Auth/database project references. The production preflight has synthetic tests rejecting reuse of the closed-test DB, non-production buckets, custom-domain routes, unsafe inline secrets, named-environment overrides, unreviewed external bindings, absent R2 caps, missing Queue/DLQ and mismatched Auth/database projects. Its read-only Hyperdrive lookup was exercised against the real closed-test config and confirmed its remote Supabase origin matches `cpweowosocjuccjsyyic`; no production config or production resource exists to validate yet. Tests also cover photo deletion, immutable event-end ZIP snapshots, post-end gallery deletion preserving an existing archive, retention cleanup, ZIP retry and dynamic ZIP filenames. The max-capacity fixture verifies 1,000 unique photos across two ZIP parts, exact manifests and peak queued ZIP stream data below 2 MiB. Migration tests confirm DB startup, release verification and Worker startup consume the same 12-version manifest, including the new tier byte-cap migration and its existing entitlement snapshots; Queue tests cover 205 jobs sent as 100/100/5, no immediate redispatch, stale-message recovery, send failure recovery, targeted consumer claims and invalid/blocked deliveries. This is local code-quality evidence plus one read-only closed-test resource identity check, not production readiness or a Cloudflare Queue/R2 load test.

## Owner-dependent steps

1. After registering the company, select legal operator name, registration/contact details, service jurisdiction, customer market and support channel. No details have been invented.
2. Check Lumiq trademark/domain availability. Replace the provisional brand if necessary.
3. Interview at least three target organizers. Compare occasional event passes with the current monthly test proposal; select one launch offer.
4. Measure storage/operations/API hosting/email/payment fees and support time. Set prices and a contingency margin only after those inputs exist.
5. After company registration, approve privacy notice, terms, refund/withdrawal rules, tax treatment, subprocessors, regions, retention, deletion/backup windows and rights-request process with qualified advice.
6. Owner's sequence: no paid production resources until after the `lumiq.cam` transition and company registration. If the domain is moved before then, it may only serve a closed pilot behind Access on isolated closed-test resources, with public registration and real guest-photo use disabled. Obtain a fresh itemized cost approval before creating paid production resources. Test resources are never a public-production substitute.
7. Nominate an operator admin through a controlled backend procedure, not a browser role field. Do not import a demo admin into the closed-test or future production database.
8. Keep the closed-test Worker behind Access until testing finishes. There is no staging deployment. Do not publish a draft privacy policy as legally approved.

## Physical-device script

1. Record phone model, OS, browser/version and test date; use only consented test photos.
2. Scan a printed QR from normal viewing distance and dim light.
3. Enter a name; take 10 consecutive photos; note every camera cancel, silent return, success and error.
4. Select 20 photos. Every selection must have a visible result; compare gallery and DB counts and check duplicates.
5. Interrupt Wi-Fi during photo, thumbnail and finalize separately. Restore network and retry. No success before both files are durable.
6. Switch apps, lock the screen, open the keyboard, rotate, test safe areas, change theme, try a long unbroken title/name.
7. Pause/resume and end the event while upload is running. Verify the server rejects incomplete new submissions after the boundary.
8. Turn guest sharing on, sort/filter, preview/download one optimized file, turn sharing off and re-open the same link.
9. Before event end, remove one consented test photo; after event end, remove another. Confirm the automatic ZIP matches the end-of-event gallery, the later gallery no longer shows its deleted photo, and the already prepared ZIP still contains it. At retention expiry, confirm the gallery and ZIP are gone and only the event name, event date/time and retention period remain visible.
10. Repeat on Android Chrome, iPhone Safari and an older/lower-memory phone. Save screenshots and counts, not credentials or signed URLs.

## Acceptance thresholds for the first controlled pilot

- Zero observed wrong-account reads, silently lost successful uploads, duplicate saved UUIDs or incomplete successful exports.
- Every test failure recoverable or reported clearly; unresolved severity-1/2 findings block release.
- One fresh tester completes the whole organizer/guest/export/support journey without developer assistance.
- Restore drill produces the same verified photo count and hashes as the backup inventory.
- Upload/gallery/export latency and storage/operations costs recorded against a written budget chosen by the owner.
- A small paid pilot is only authorized after legal, provider and reliability gates pass. This task has not authorized or performed it.

## Evidence to record

For each gate: date, code version, environment ID, exact test steps, expected/actual, screenshots/log reference, tester and unresolved issue. Do not mark a checkbox based solely on code existing.
