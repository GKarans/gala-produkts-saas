import {spawn} from 'node:child_process';import {readFile} from 'node:fs/promises';import {createReadStream} from 'node:fs';import path from 'node:path';import {S3Client,PutObjectCommand,ListObjectsV2Command} from '@aws-sdk/client-s3';
if(process.env.PLATFORM_RESTORE_DRILL!=='EMPTY-ISOLATED-TARGET')throw new Error('Set PLATFORM_RESTORE_DRILL=EMPTY-ISOLATED-TARGET only for a new empty drill environment.');
const root=path.resolve(process.argv[2]||'');if(!process.argv[2])throw new Error('Usage: node platform/scripts/restore-drill.mjs <backup-directory>');const database=process.env.PLATFORM_DATABASE_URL,bucket=process.env.PLATFORM_R2_BUCKET,endpoint=process.env.PLATFORM_R2_ENDPOINT;if(!database||!bucket||!endpoint)throw new Error('Load the empty restore target credentials.');
const s3=new S3Client({region:'auto',endpoint,credentials:{accessKeyId:process.env.PLATFORM_R2_ACCESS_KEY_ID,secretAccessKey:process.env.PLATFORM_R2_SECRET_ACCESS_KEY}}),existing=await s3.send(new ListObjectsV2Command({Bucket:bucket,MaxKeys:1}));if(existing.KeyCount)throw new Error('Restore target bucket is not empty.');
const pg=spawn('pg_restore',['--exit-on-error','--no-owner','--no-acl','--dbname',database,path.join(root,'database.dump')],{stdio:'inherit',windowsHide:true});if(await new Promise(resolve=>pg.on('exit',resolve))!==0)throw new Error('Database restore failed.');
const manifest=JSON.parse(await readFile(path.join(root,'manifest.json'),'utf8'));for(const object of manifest.objects)await s3.send(new PutObjectCommand({Bucket:bucket,Key:object.key,Body:createReadStream(path.join(root,object.file))}));console.log(`Restore drill loaded the database and ${manifest.objects.length} objects. Run migrations, application smoke tests and checksum verification before recording success.`);
